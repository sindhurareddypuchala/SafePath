# SafePath Root Test Suite

This directory contains cross-cutting integration, load, and end-to-end system tests.

## Test Suites
* `/backend/tests`: Fast unit tests, API integration tests, risk formula validations.
* `/frontend/tests`: Component unit tests and render tests.
* `/tests`: End-to-end integration tests (e.g. Playwright browser/journey scenarios).
